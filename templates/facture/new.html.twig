{% extends 'base.html.twig' %}

{% block title %}New Facture{% endblock %}

{% block body %}
<div class="container d-flex align-items-center justify-content-center" style="height: 200vh;"> <!-- Container Bootstrap pour centrer le contenu -->
    <div class="text-center">
    <h1 class="custom-title" style="color: #c43ddb;">Create new Facture</h1>

    <div style="width: 300px; margin: 0 auto; padding:70px"> <!-- Centrage horizontal du formulaire -->

    {{ include('facture/_form.html.twig') }}
    </div>
    <a href="{{ path('app_facture_index') }}">back to list</a>

     </div>
</div>
    {% endblock %}
