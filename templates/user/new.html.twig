{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        /* Styles for centering content */
        .center-content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Customize the form styles */
        .custom-form {
            /* Add your custom form styles here */
        }
    </style>
{% endblock %}

{% block body %}
    <div class="center-content">
        <h1>Create new User</h1>

        <div class="custom-form" style="width: 300px; margin: 0 auto; padding:70px">
            {{ include('user/_form.html.twig') }}
        </div>

        <a href="{{ path('app_user_index') }}">back to list</a>
    </div>
{% endblock %}
